server:
  port: 8080

spring:
  application:
    name: Project-JWT

  datasource:
    url: jdbc:mysql://localhost:3306/auth_db_spring?useSSL=false&serverTimezone=Asia/Ho_Chi_Minh
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: hieu102056@donga.edu.vn
    password: wwbo bpjv dyfo xgzu
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

jwt:
  secret: dev-secret-key
  expiration: 3600000
